import psycopg2


def get_connection():
    return psycopg2.connect(
        dbname="english_bot",
        user="postgres",
        password="1234",  # ← поставь свой пароль
        host="localhost",
        port="5432"
    )
